{% extends "base.jinja" %}

{% block title %}Error {{ error_code }}{% endblock %}
{% block head %}
    <link rel="stylesheet" href="{{ static('/css/error.css') }}">
{% endblock %}

{% block body %}
    	<div id="notfound">
		<div class="notfound">
			<div class="notfound-404">
				<h1>Oops!</h1>
				<h2>{{ error_code }} - {{ error_message }}</h2>
			</div>

			<a href="{{url('home') }}" class="btn btn-primary">{{ _("Return to main") }}</a>
		</div>
	</div>
{% endblock %}